from django.apps import AppConfig


class PayPeriodsConfig(AppConfig):
    name = 'stla_lite.payperiods'
    verbose_name = "Pay Periods"

    def ready(self):
        """
        Placeholder.
        """
        pass
